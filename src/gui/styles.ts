/** Shared class strings so visual choices live in one place. Compose with other classes as needed. */

/** Small uppercase eyebrow label — sidebar headers, panel labels, sub-section labels. */
export const SECTION_LABEL = 'text-xs font-semibold uppercase tracking-wide text-gray-500';

/** Larger uppercase heading — section titles inside cards. */
export const SECTION_HEADING = 'text-sm font-semibold uppercase tracking-wide text-gray-500';

/** Muted italic used for "nothing here yet" messages. */
export const EMPTY_STATE = 'text-sm text-gray-400 italic';
